import 'package:flutter/material.dart';

class DiseasesDetectionPage extends StatelessWidget {
  const DiseasesDetectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('No content'),
        ),
      ),
    );
  }
}
